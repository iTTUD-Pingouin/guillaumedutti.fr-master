import "bootstrap";
import Typed from 'typed.js';

let options = {
  strings: ["Référencement naturel", "Création de sites Web", "Google et Facebook Ads"],
  typeSpeed: 40,
  loop: true,
  backDelay: 3000,
  backSpeed: 20,
  showCursor: false,
}

let typed = new Typed(".main-title", options);
